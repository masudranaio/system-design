---
title: "Amazon Locker — High-Level Design"
module: "04-case-studies/amazon-locker"
---

This lesson zooms out from a single locker station to the network that
operates thousands of them. It builds directly on the object model from
the [Amazon Locker LLD](/case-studies/amazon-locker/lld) — the same
`Compartment`, `Package`, and `AccessToken` concepts still apply inside
one station — but everything about *finding* a station, *coordinating*
reservations across it, and *staying available* when a station drops
offline is a different problem, and that's what this lesson covers.

## Problem framing

The LLD asks: given one station with 30-150 compartments, how do you
deposit and retrieve a package correctly? The HLD asks a different
question: given a few thousand of those stations spread across a
country, how does a customer in Austin find a station near their office
with an open medium compartment, reserve it before anyone else does, and
still get service if that exact station's cellular modem drops for an
hour on a Saturday?

That reframes the problem three ways:

- **Geo-discovery.** "Find a locker near me with capacity" is a spatial
  query over thousands of moving-target availability counts, not a
  lookup on one station's compartment list.
- **Coordinated reservation.** Two customers browsing the same
  neighborhood can try to claim the same compartment within milliseconds
  of each other, and the system spans many app servers with no single
  in-memory lock to serialize them against.
- **Fleet-wide connectivity.** Every station is a physical device on a
  cellular or Wi-Fi uplink, not a service behind a stable data-center
  network — some fraction of the fleet is always mid-reconnect, and the
  design has to keep working through that instead of treating it as an
  outage.

None of this is "the LLD problem at bigger numbers." It's a distributed
IoT and reservation-coordination problem where the single-station logic
from the LLD is just one component running inside each station.

<QuizItem
  question="Why isn't the HLD just 'the LLD problem, but with more stations'?"
  answer="The LLD's hard problem is single-station concurrency (two couriers, one compartment). The HLD's hard problems are different in kind: spatial search across stations that aren't co-located, reservation coordination across many stateless app servers instead of one in-memory station, and tolerating individual stations going offline — none of which exist at single-station scale."
/>

## Requirements & capacity estimate

**Functional requirements:**

- Locker discovery by geo-radius plus a package-size filter
- Atomic slot reservation that prevents two concurrent requests from
  double-booking the same compartment
- Deposit and pickup at any of the network's stations
- Device health and telemetry monitoring per station
- Over-the-air (OTA) firmware updates pushed to the fleet
- Returns coordination, including scheduling carrier pickup of
  return-pending packages

**Non-functional requirements:**

- Peak load of roughly **500,000 QPS** on the capacity-reservation path
- Availability queries answered in **under 100ms**
- Physical unlock latency of **2-3 seconds** from code entry
- **>95% cache hit rate** on the availability-query path
- **24/7** operation with graceful degradation when a station is offline
- Compartment mix of roughly 40% small / 35% medium / 20% large / 5%
  oversized, with 30-150 compartments per station
- Prime-Day-style events causing **10-20x** volume spikes in affected
  metros

**Out of scope for this lesson:** payment processing for locker rental,
full account/auth internals, notification-delivery internals (SMS vs.
push vs. email), and route optimization for delivery agents — these are
services this design calls into, not sections it designs.

<KeyStat
  value="~500,000 QPS"
  label="Peak availability-query load"
  detail="Baseline write volume (actual deposits/pickups) runs ~2,000 events/sec network-wide. Each completed event is preceded by many availability reads — a customer pans a map, filters by size, re-checks after a first choice fills up — call that read amplification ~15x, putting ordinary-day read traffic around 30,000 QPS. Layer a Prime-Day-style ~17x spike (inside the stated 10-20x range) on top: 30,000 x 17 ≈ 500,000 QPS. The read path, not the write path, is what has to survive the event."
/>

<KeyStat
  value="~450,000 compartments"
  label="Network-wide compartment inventory"
  detail="~5,000 stations (consistent with 'thousands') x 90 compartments/station (midpoint of the 30-150 range) ≈ 450,000 compartments — roughly 180,000 small, 157,500 medium, 90,000 large, 22,500 oversized, following the stated size mix. That total is the ceiling the caching and reservation design below has to serve queries against without hitting the database on every read."
/>

## Core content

### Network architecture

Splitting the network into three focused diagrams — request routing,
the data/cache tier, and the IoT fleet channel — instead of one large
one; each carries its own explanatory weight and the IoT channel in
particular solves a different problem (device connectivity, not request
routing) than the other two.

<D2Diagram
  title="Client and service layer"
  type="architecture"
  chart={`direction: right

CustomerApp: Customer App {
  style.fill: "#3b6fd6"
  style.font-color: "#ffffff"
}
AgentApp: Delivery Agent App {
  style.fill: "#3b6fd6"
  style.font-color: "#ffffff"
}
Kiosk: In-Station Touchscreen {
  style.fill: "#3b6fd6"
  style.font-color: "#ffffff"
}
GW: API Gateway {
  style.fill: "#5b4fbf"
  style.font-color: "#ffffff"
}
LMS: Locker Management Service {
  style.fill: "#0e7c86"
  style.font-color: "#ffffff"
}
CRS: Capacity Reservation Service (hot path) {
  style.fill: "#0e7c86"
  style.font-color: "#ffffff"
}
PKGS: Package Service {
  style.fill: "#0e7c86"
  style.font-color: "#ffffff"
}
AuthS: Auth Service {
  style.fill: "#0e7c86"
  style.font-color: "#ffffff"
}
NotifS: Notification Service {
  style.fill: "#0e7c86"
  style.font-color: "#ffffff"
}
RetS: Returns Service {
  style.fill: "#0e7c86"
  style.font-color: "#ffffff"
}

CustomerApp -> GW
AgentApp -> GW
Kiosk -> GW
GW -> LMS: station/compartment state, health
GW -> CRS: reserve/availability, 500K QPS peak
GW -> PKGS: package lifecycle
GW -> AuthS
GW -> RetS
PKGS -> NotifS: deposit/pickup events
RetS -> NotifS: return-ready events
`}
/>

_The gateway fans requests out to five focused services instead of one
monolith. Capacity Reservation Service gets its own hot-path lane
because it alone has to sustain 500,000 QPS; Locker Management, Package,
Auth, and Returns run at far lower frequency and would only add
contention if they shared a connection pool with reservation._

<D2Diagram
  title="Cache and data tier"
  type="architecture"
  chart={`direction: right

CRS: Capacity Reservation Service {
  style.fill: "#0e7c86"
  style.font-color: "#ffffff"
}
LMS: Locker Management Service {
  style.fill: "#0e7c86"
  style.font-color: "#ffffff"
}
PKGS: Package Service {
  style.fill: "#0e7c86"
  style.font-color: "#ffffff"
}
RetS: Returns Service {
  style.fill: "#0e7c86"
  style.font-color: "#ffffff"
}
L1: L1 in-process cache {
  style.fill: "#b8722a"
  style.font-color: "#ffffff"
}
L2: L2 Redis {
  style.fill: "#b8722a"
  style.font-color: "#ffffff"
}
L3: L3 regional snapshot {
  style.fill: "#b8722a"
  style.font-color: "#ffffff"
}
PG: Postgres, geo-sharded {
  style.fill: "#b23a48"
  style.font-color: "#ffffff"
}
TSDB: Time-series store (telemetry) {
  style.fill: "#b23a48"
  style.font-color: "#ffffff"
}

CRS -> L1 -> L2 -> L3 -> PG: cache-miss fallthrough
LMS -> PG: station/compartment config
PKGS -> PG: package records
RetS -> PG: return records
LMS -> TSDB: heartbeat, door sensors, power
`}
/>

_Two data stores, not one: Postgres holds transactional state where
correctness beats write throughput, and a time-series store holds
telemetry, where writes are small, high-volume, and almost always
queried as "show me the last N minutes for station X" — a shape
relational tables handle poorly at fleet scale._

<D2Diagram
  title="IoT fleet connectivity"
  type="architecture"
  chart={`direction: right

Station: Locker Station {
  style.fill: "#3b6fd6"
  style.font-color: "#ffffff"
}
LMS: Locker Management Service {
  style.fill: "#0e7c86"
  style.font-color: "#ffffff"
}
CRS: Capacity Reservation Service {
  style.fill: "#0e7c86"
  style.font-color: "#ffffff"
}
TSDB: Time-series store {
  style.fill: "#b23a48"
  style.font-color: "#ffffff"
}

Station -> LMS: MQTT telemetry (heartbeat, door sensors, power, firmware version)
LMS -> TSDB: write
CRS -> Station: unlock command + sync, TLS/X.509 device auth
`}
/>

_Station telemetry flows over its own async channel so a spike in
device chatter can't compete with customer-facing reservation traffic
for the same connection pool — the reasoning behind splitting Capacity
Reservation Service out from Locker Management Service in the first
diagram, restated for the fleet side._

Two design choices are doing the heavy lifting across these three
diagrams:

- **Capacity Reservation Service is split out from Locker Management
  Service**, even though both ultimately touch compartment state.
  Reservation is the narrow, latency-critical hot path (sub-100ms reads,
  500,000 QPS peak); Locker Management handles lower-frequency
  station-health and configuration operations. Splitting them means a
  burst of device telemetry or a firmware rollout can't compete for the
  same connection pool as a customer trying to reserve a compartment.
- **Two data stores, not one.** Postgres for transactional correctness,
  a time-series store for high-volume small-point telemetry — detailed
  above.

<QuizItem
  question="Why does the Capacity Reservation Service sit on its own hot path separate from Locker Management Service, even though both touch compartment state?"
  answer="Reservation is the highest-QPS, most latency-sensitive path (500,000 QPS peak, sub-100ms target) while Locker Management handles far less frequent station/health operations. Separating them means a health-monitoring or firmware-rollout spike can't degrade reservation latency, and each service scales independently to its own load profile."
/>

### Geo-discovery and reservation under contention

"Find lockers within 2 miles with a medium slot open" is a spatial range
query, and running it as a full scan over 450,000 compartments per
request is what makes 100ms latency impossible at 500,000 QPS. The fix
is a **geohash** — an encoding that turns a latitude/longitude pair into
a short string (e.g. `9q8yy`) such that nearby points share a string
prefix. "Find everything within 2 miles" becomes "find everything whose
geohash starts with this prefix," which is an indexed prefix lookup
instead of a geometric scan over every station's coordinates.

{/* concept-dependency: HLD-11 not yet built, explained inline */}

<D2Diagram
  title="Geohash prefix lookup vs. geometric scan"
  type="architecture"
  chart={`direction: right

Query: "Within 2 miles of (30.27, -97.74)"
Naive: Geometric distance scan (all 450K compartments) {
  style.fill: "#b23a48"
  style.font-color: "#ffffff"
}
Geohash: "Geohash prefix index lookup (9v6k...)" {
  style.fill: "#0e7c86"
  style.font-color: "#ffffff"
}
Result: Matching stations, ranked

Query -> Naive: O(n) scan, too slow at 500K QPS
Query -> Geohash: indexed prefix seek
Geohash -> Result
`}
/>

_The naive scan and the geohash path both answer the same query — the
diagram exists to make the "why not just scan" question concrete rather
than asserted._

Once a customer picks a station, the reservation itself has to survive
concurrent attempts. Three mechanisms work together, each solving a
different layer of the contention problem:

- **Optimistic concurrency control (OCC).** Every compartment row
  carries a `version` number; a reservation reads the current version,
  then issues `UPDATE compartments SET status = 'reserved', version =
  version + 1 WHERE id = ? AND version = ?`. If another request already
  reserved that compartment, the version no longer matches, the update
  affects zero rows, and the service returns a conflict — the customer
  is shown the next-nearest match instead.
- **Why OCC over a pessimistic lock.** A pessimistic lock (acquire a
  lock on the row, block other requests, release after the transaction)
  pays a blocking cost on *every* request in exchange for correctness
  that's only actually needed on the rare request that collides. Given
  that any single compartment is contended by two customers only
  occasionally, OCC pays the cost — a retry — only when a collision
  actually happens.
- **Regional reservation queues at extreme throughput.** Even OCC needs
  help at 500,000 QPS: if every reservation attempt across the whole
  network competes for connections against every other attempt, the
  database connection pool becomes the bottleneck regardless of how
  cheap each individual update is. The design pre-allocates regional
  reservation queues — requests are bucketed by geographic region
  (roughly metro-area granularity) into separate worker pools, so
  contention is bounded to reservations that could plausibly collide
  (same station, same region) rather than serialized against unrelated
  reservations on the other side of the country.

{/* concept-dependency: HLD-10 not yet built, explained inline */}

<D2Diagram
  title="Optimistic concurrency: two customers, one compartment"
  type="sequence"
  chart={`shape: sequence_diagram

CustomerA: Customer A {
  style.fill: "#3b6fd6"
  style.font-color: "#ffffff"
}
CustomerB: Customer B {
  style.fill: "#3b6fd6"
  style.font-color: "#ffffff"
}
CRS: Capacity Reservation Service {
  style.fill: "#0e7c86"
  style.font-color: "#ffffff"
}
PG: Postgres compartment row (v=3) {
  style.fill: "#b23a48"
  style.font-color: "#ffffff"
}

CustomerA -> CRS: reserve(compartmentId), read version=3
CustomerB -> CRS: reserve(compartmentId), read version=3
CRS -> PG: "UPDATE ... SET version=4 WHERE version=3 (A)"
PG -> CRS: 1 row updated, version now 4
CRS -> CustomerA: reservation confirmed
CRS -> PG: "UPDATE ... SET version=4 WHERE version=3 (B)"
PG -> CRS: 0 rows updated, version already 4
CRS -> CustomerB: 409 conflict, next-nearest offered
`}
/>

<QuizItem
  question="Two customers in the same city both try to reserve the last available medium compartment at the same station within a 50ms window. Under optimistic concurrency control, what happens?"
  answer="Both read the compartment row at version v. Whichever conditional UPDATE ... WHERE version = v commits first wins, and the row moves to v+1. The second UPDATE matches zero rows because the version already changed, so that request gets a conflict response and the losing customer is offered the next-nearest available compartment — no lock was held while either request was in flight."
/>

### Multi-level caching for the read path

At 500,000 QPS peak with a sub-100ms target, hitting Postgres on every
availability read is not an option — even a well-indexed geo-sharded
query at that volume saturates connection pools long before it
saturates disk I/O. A real caching deep dive needs more than "put Redis
in front of it": tier design, write propagation, and stampede risk are
three separate mechanisms, and an interview conversation branches into
all three.

<D2Diagram
  title="Availability query — four-tier cache read path"
  type="architecture"
  chart={`direction: right

Req: Availability query
L1: L1 in-process (under 1ms, TTL 5-10s) {
  style.fill: "#b8722a"
  style.font-color: "#ffffff"
}
L2: L2 Redis (1-5ms, write-through) {
  style.fill: "#b8722a"
  style.font-color: "#ffffff"
}
L3: L3 regional snapshot (refreshed 5-15s) {
  style.fill: "#b8722a"
  style.font-color: "#ffffff"
}
L4: L4 geo-sharded Postgres, source of truth {
  style.fill: "#b23a48"
  style.font-color: "#ffffff"
}
Resp: Response

Req -> L1
L1 -> Resp: hit
L1 -> L2: miss
L2 -> Resp: hit
L2 -> L3: miss
L3 -> Resp: hit
L3 -> L4: miss or stale
L4 -> Resp: backfill L1, L2, L3
`}
/>

_Each tier absorbs the volume the one behind it can't sustain — L1 is
per-instance and free of network hops, L2 is shared across instances so
a miss on one server can hit on another, and L3 exists because even a
fast key-value hit still costs a scatter-gather across many keys to
build a ranked "nearest N compartments" answer; L3 precomputes that
answer directly._

{/* concept-dependency: HLD-06 not yet built, explained inline */}
{/* concept-dependency: HLD-07 not yet built, explained inline */}

**Branch 1 — write propagation.** L2 uses **write-through** caching:
when a reservation changes a compartment's status, the write goes to
Redis and Postgres together (rather than **cache-aside**, where the
cache is only populated lazily on the next read miss), because a
reservation-critical path can't afford to serve a stale "available"
answer to the very next customer who reads it.

**Branch 2 — geo-sharding at the source of truth.** L4, Postgres, is
**geo-sharded** — partitioned by region so a query for Chicago-area
lockers only ever touches the Chicago shard's data and its read
replicas, never scanning rows for stations 2,000 miles away. That >95%
cache hit rate target means fewer than 1 in 20 requests ever reaches L4
at all; the shard layout is there for the requests that do, and for the
L3 snapshot jobs that periodically rebuild from it.

**Branch 3 — cache stampede risk on a hot metro.** If a heavily-trafficked
region's L3 snapshot expires right as a Prime-Day-style spike hits it,
every request that misses in that instant could hit L4 simultaneously.
The mitigation is a single-flight recompute: only one request per
region rebuilds the L3 snapshot on a miss, while the rest keep serving
the (seconds-stale) snapshot they already have until the rebuild
completes — the same technique used for hot-key stampedes in any
high-QPS cache-aside system, applied here to a regional snapshot instead
of a single key.

<QuizItem
  question="Why does the L3 regional snapshot exist at all if L2 Redis already caches the same underlying data?"
  answer="A single Redis GET is fast, but answering 'nearest N available medium compartments near this point' from raw per-compartment keys still requires a scatter-gather across many keys per request. L3 precomputes and stores that ranked answer directly, refreshed every 5-15 seconds, so the hot path becomes a single lookup instead of an aggregation done on every request."
/>

### IoT fleet and offline resilience

Every station is a physical device on a cellular or Wi-Fi uplink, and at
fleet scale some stations are always mid-reconnect. Two channels connect
a station to the rest of the system, covered in the architecture
diagrams above:

- An **asynchronous telemetry channel over MQTT** — a lightweight
  publish/subscribe protocol built for devices with intermittent
  connectivity and limited bandwidth. Stations publish heartbeat,
  door-sensor events, power status, and firmware version; the Locker
  Management Service subscribes and writes them into the time-series
  store.
- A **synchronous unlock-command path**, authenticated per-device with
  **TLS/X.509** — a certificate-based identity scheme where each station
  holds a unique certificate the central system verifies before
  trusting a command or accepting a report, so a compromised or spoofed
  device can't unlock compartments it doesn't own.

{/* concept-dependency: HLD-10 not yet built, explained inline */}

Neither channel assumes the station is always reachable:

<D2Diagram
  title="Offline resilience — station-side mechanisms"
  type="architecture"
  chart={`direction: down

Station: Locker Station (uplink down) {
  style.fill: "#3b6fd6"
  style.font-color: "#ffffff"
}
CodeCache: Local cache of active pickup codes {
  style.fill: "#b8722a"
  style.font-color: "#ffffff"
}
RetryQ: Local retry queue, exponential backoff {
  style.fill: "#4b5262"
  style.font-color: "#ffffff"
}
Battery: Battery backup (30-60 min)
Central: Central system (reconciles on reconnect) {
  style.fill: "#0e7c86"
  style.font-color: "#ffffff"
}

Station -> CodeCache: pickup validated locally, no round trip needed
Station -> RetryQ: deposit/pickup events queued while offline
Battery -> Station: keeps local compute alive through short power blips
RetryQ -> Central: flush + reconcile once connectivity returns
`}
/>

- A station keeps a **local cache of active pickup codes** synced ahead
  of time, so a customer entering a valid code can pick up a package
  even if the station's uplink is down at that exact moment — the
  station validates the code against its own cache rather than calling
  the central service synchronously for every pickup.
- Deposit and pickup events that happen offline go into a **local retry
  queue with exponential backoff**: the station keeps attempting to
  flush the queue to the central system, waiting longer between attempts
  each time it fails, until connectivity returns and the events
  **reconcile** — the central system replays them against the
  source-of-truth database and resolves anything that conflicted with
  events it saw from elsewhere in the meantime.
- A **battery backup rated for 30-60 minutes** keeps the station's local
  compute (not the compartment locks themselves) running through short
  power blips so the local cache and queue survive them.

Lock hardware itself makes a physical trade-off:

<CompareTable
  title="Lock hardware behavior on power loss"
  rows={[
    {
      approach: "Fail-secure",
      pros: ["Stays locked on power loss, protecting whatever is already stored"],
      cons: ["Strands a courier's undelivered package and anyone mid-pickup during the outage"],
      chosenBecause: "A network built for 24/7 self-service leans fail-secure by default — a stranded package is recoverable; a stolen one isn't.",
    },
    {
      approach: "Fail-open",
      pros: ["Guarantees access even during a power outage"],
      cons: ["Risks theft during the outage window, since the lock releases with no supervision"],
    },
  ]}
/>

Neither is uniformly correct — stations storing higher-value contents
may configure specific compartments fail-open under supervised
conditions instead of applying one policy network-wide.

<QuizItem
  question="A station's cellular connection drops for 40 minutes during a busy Saturday. Why does pickup still work during that window?"
  answer="The station keeps a local cache of active pickup codes synced before it went offline, so it can validate a code and physically unlock the compartment without calling the central service. The resulting deposit/pickup event is queued locally with exponential-backoff retry and reconciled against the source of truth once connectivity returns."
/>

## Trade-offs

**Consistency vs. availability for locker state.** The design chooses
**eventual consistency**: a station keeps accepting deposits and
pickups while offline and reconciles afterward, over **strong
consistency**, where a deposit blocks until the central system confirms
it. Strong consistency would guarantee no station ever diverges from the
source of truth, but it means a station with a flaky uplink stops
accepting packages entirely during every blip — for a fleet where some
fraction of stations are always mid-reconnect, that's a worse failure
mode than the rare double-booking eventual consistency has to catch and
resolve on reconciliation.

{/* concept-dependency: HLD-01 not yet built, explained inline */}

This is the same tension the CAP theorem names for any distributed
system: when a network partition happens (here, a station losing its
uplink), you can't have both full consistency and full availability
across the partition, so the design has to pick. Amazon Locker picks
availability at the station and accepts short-lived inconsistency,
because a locker a courier can't currently reach is worse than a locker
whose state briefly disagrees with the network.

<CompareTable
  title="Trade-offs across the network design"
  rows={[
    {
      approach: "Local code verification (chosen)",
      pros: ["Makes offline pickup possible at all", "No round trip on the pickup hot path"],
      cons: ["A code revoked centrally (e.g. a theft report) doesn't take effect at that station until its next sync"],
      chosenBecause: "Offline pickup capability outweighs the delayed-revocation window, which is short relative to sync frequency.",
    },
    {
      approach: "Centralized code verification",
      pros: ["Revocation takes effect immediately, everywhere"],
      cons: ["Pickup fails outright whenever the station can't reach the network — the exact case this design has to survive"],
    },
    {
      approach: "Conservative reservation hold (chosen)",
      pros: ["Simple, never double-books", "Guaranteed valid until the customer uses or abandons it"],
      cons: ["Ties up a compartment for the full hold window even for no-shows, lowering utilization"],
      chosenBecause: "Simplicity and zero-risk are worth more than utilization at base scale; probabilistic release is a genuine extension under sustained capacity pressure.",
    },
    {
      approach: "Aggressive, probabilistic release",
      pros: ["Raises utilization by freeing reservations early based on predicted low follow-through"],
      cons: ["Introduces a real (if small) chance of releasing a compartment out from under a late-arriving customer"],
    },
    {
      approach: "Precomputed L3 regional snapshot (chosen)",
      pros: ["Sustains 500,000 QPS", "Single lookup, no per-request aggregation"],
      cons: ["Answers can be up to 5-15 seconds stale"],
      chosenBecause: "On-demand scatter-gather-and-rank at 500,000 QPS is the same cost problem caching exists to avoid — not a real alternative at this volume.",
    },
    {
      approach: "On-demand computation",
      pros: ["Eliminates staleness entirely"],
      cons: ["Doing the ranked scatter-gather fresh on every request at 500,000 QPS still ends up needing heavy caching in front of it"],
    },
  ]}
/>

<QuizItem
  question="Why does the Capacity Reservation Service choose eventual consistency for station state instead of blocking every deposit on central confirmation?"
  answer="Blocking on central confirmation means a station that's briefly offline can't accept any deposits at all. Eventual consistency trades a rare double-booking — caught and resolved on reconciliation — for keeping every station able to accept packages through short connectivity blips, which is the much more common case at fleet scale."
/>

## Worked example

Trace one customer's flow end to end across three focused diagrams:
discovery through the caching tiers, reservation and a courier's deposit
through a brief offline window, and reconciliation back to the source of
truth.

<D2Diagram
  title="Step 1 — discovery through the cache tiers"
  type="sequence"
  chart={`shape: sequence_diagram

Customer: Customer {
  style.fill: "#3b6fd6"
  style.font-color: "#ffffff"
}
App: Customer App {
  style.fill: "#3b6fd6"
  style.font-color: "#ffffff"
}
GW: API Gateway {
  style.fill: "#5b4fbf"
  style.font-color: "#ffffff"
}
L3: L3 Regional Snapshot {
  style.fill: "#b8722a"
  style.font-color: "#ffffff"
}
Redis: L2 Redis {
  style.fill: "#b8722a"
  style.font-color: "#ffffff"
}
PG: L4 Postgres (geo-shard) {
  style.fill: "#b23a48"
  style.font-color: "#ffffff"
}

Customer -> App: "Lockers near me, medium size"
App -> GW: GET /lockers?lat,lng,radius,size=medium
GW -> L3: geohash prefix lookup
L3 -> GW: "ranked candidates, seconds-stale (hit)"
GW -> Redis: "on L3 miss: geohash lookup"
Redis -> GW: "ranked candidates (hit)"
Redis -> PG: "on Redis miss: geo-sharded range query"
PG -> GW: ranked candidates
GW -> Redis: backfill on miss path
GW -> L3: backfill on miss path
GW -> App: candidate stations
`}
/>

<D2Diagram
  title="Step 2 — reserve, then deposit through an offline blip"
  type="sequence"
  chart={`shape: sequence_diagram

App: Customer App {
  style.fill: "#3b6fd6"
  style.font-color: "#ffffff"
}
CRS: Capacity Reservation Service {
  style.fill: "#0e7c86"
  style.font-color: "#ffffff"
}
PG: L4 Postgres {
  style.fill: "#b23a48"
  style.font-color: "#ffffff"
}
Station: "Locker Station #4471" {
  style.fill: "#3b6fd6"
  style.font-color: "#ffffff"
}

App -> CRS: reserve(compartmentId, version=v)
CRS -> PG: "UPDATE compartments SET status='reserved', version=v+1 WHERE version=v"
PG -> CRS: 1 row updated, version now v+1
CRS -> App: reservation confirmed, access code issued
CRS -> Station: sync reservation + code to local cache
Station -> Station: note -- cellular connectivity drops
Station -> Station: note -- courier arrives, enters code
Station -> Station: validate code against local cache, offline
Station -> Station: unlock, mark compartment occupied locally
Station -> Station: queue deposit-confirmed event, retry with backoff
`}
/>

<D2Diagram
  title="Step 3 — reconciliation once connectivity returns"
  type="sequence"
  chart={`shape: sequence_diagram

Station: "Locker Station #4471" {
  style.fill: "#3b6fd6"
  style.font-color: "#ffffff"
}
CRS: Capacity Reservation Service {
  style.fill: "#0e7c86"
  style.font-color: "#ffffff"
}
PG: L4 Postgres {
  style.fill: "#b23a48"
  style.font-color: "#ffffff"
}
Redis: L2 Redis {
  style.fill: "#b8722a"
  style.font-color: "#ffffff"
}
L3: L3 Regional Snapshot {
  style.fill: "#b8722a"
  style.font-color: "#ffffff"
}
Notif: Notification Service {
  style.fill: "#0e7c86"
  style.font-color: "#ffffff"
}
Customer: Customer {
  style.fill: "#3b6fd6"
  style.font-color: "#ffffff"
}

Station -> Station: note -- connectivity restored
Station -> CRS: flush queued deposit-confirmed event
CRS -> PG: reconcile: confirm occupancy in source of truth
CRS -> Redis: invalidate stale entry
CRS -> L3: invalidate stale entry
CRS -> Notif: trigger pickup-code notification
Notif -> Customer: pickup code, 3-day window
`}
/>

_Nothing here is hypothetical padding — every step maps to a piece of
the design above: the geohash-prefix search, the four-tier cache falling
through to Postgres on a cold path, the versioned conditional update
that would reject a losing concurrent reservation, and the
offline-then-reconcile flow that keeps the deposit valid even though the
station couldn't reach the network at the moment it happened._

Two details are easy to miss on a first read. First, the reservation
sync to the station happens **before** the courier arrives, not at
deposit time — that's what makes the offline deposit possible at all;
if the station only learned about the reservation when the courier
showed up, an offline station couldn't validate a delivery it had never
heard of. Second, the cache invalidation happens explicitly, as its own
step, rather than waiting for L2's TTL or L3's next refresh cycle — a
compartment that's now physically occupied can't be left showing
"available" for even the 5-15 seconds until the next natural refresh,
because that's exactly the window a second customer could reserve a slot
that's already full.

## Interview angle

**"How do you prevent two customers from reserving the same compartment
at the same time across two different app servers?"** Point at the
versioned conditional update: no server holds an in-memory lock, so
"two different app servers" isn't actually a special case — every
reservation attempt, from any server, races against the same database
row's version number, and the database itself is what arbitrates the
winner.

**"A locker station loses internet for an hour during a busy period —
what happens to in-flight pickups and deposits?"** Walk through the
offline-resilience deep dive: local code cache for pickups, local retry
queue with backoff for deposits, reconciliation once connectivity
returns. The interviewer is checking whether you reach for "the station
just fails" as the default answer, or whether you've designed the
offline path as a first-class case instead of an afterthought.

**"Prime Day causes a 15x spike in one metro area — where does the
system bend or break first, and how do you protect it?"** The read path
bends first, not the write path — the back-of-envelope math shows read
volume amplified well beyond actual deposit/pickup volume, so cache
eviction pressure and L3 staleness show up before reservation throughput
does. The answer isn't "add more app servers": it's regional isolation
(the affected metro's shard and cache tier absorb the spike without
starving other regions) plus, if sustained, temporarily widening L3's
refresh interval to trade a little more staleness for headroom.

## Practice & Self-Check

### Recap quiz

<QuizItem
  question="What problem does geohashing solve in the locker-discovery path?"
  answer="It turns a 'find compartments within N miles' query into a prefix-range lookup by encoding latitude/longitude into a short string where nearby points share string prefixes, avoiding an expensive geometric scan across every station's coordinates."
/>

<QuizItem
  question="Name the four cache tiers in the capacity-reservation read path, in order, with their approximate latency."
  answer="L1 in-process (under 1ms, TTL 5-10s) -> L2 Redis (1-5ms, write-through) -> L3 precomputed regional snapshot (refreshed every 5-15s) -> L4 geo-sharded Postgres, the source of truth."
/>

<QuizItem
  question="Why is optimistic concurrency control preferred over a pessimistic lock for compartment reservation?"
  answer="Contention on any single compartment is rare relative to total reservation volume, so paying a locking cost (blocking other requests, holding a connection) on every single request is wasteful. OCC only pays a retry cost on the rare request that actually collides."
/>

<QuizItem
  question="A courier deposits a package while a station is mid-reconnect after an outage. What keeps that deposit from being lost?"
  answer="The deposit event goes into a local retry queue with exponential backoff on the station itself; it isn't considered complete network-wide until it's flushed and the central system acknowledges it during reconciliation."
/>

<QuizItem
  question="Why would fail-secure lock hardware be the wrong default for every single compartment in the network, even though it protects packages best?"
  answer="Fail-secure keeps compartments locked on power loss, which strands both a courier's undelivered package and any already-stored package behind a dead battery. For a network built around 24/7 self-service access, that trade is better made per-compartment based on contents risk than applied uniformly network-wide."
/>

<QuizItem
  question="During a Prime-Day-style 15x spike in one metro, why does the read path (discovery/availability) bend before the write path (actual deposit/pickup)?"
  answer="Read volume amplifies far more than write volume during a spike, since many customers browse and re-check availability for every one who completes a deposit or pickup. The caching tiers absorbing that read volume show pressure — L3 staleness, L1/L2 eviction — before reservation throughput does."
/>

<QuizItem
  question="Scenario: a customer's app shows a locker as available, but their reservation attempt fails immediately after. What's the most likely explanation given this lesson's caching design?"
  answer="The app read from a cache tier (L1, L2, or L3) that was seconds behind the source of truth — someone else's reservation had already committed in Postgres but hadn't propagated to that tier yet. This is the accepted cost of trading perfect freshness for the throughput the cache tiers provide."
/>

<QuizItem
  question="Why does the Capacity Reservation Service explicitly invalidate the L2 and L3 cache entries after reconciliation, rather than waiting for their normal TTL or refresh cycle?"
  answer="Waiting for the natural refresh (5-15s for L3, TTL for L1/L2) would keep serving a stale 'available' answer for a compartment that's now physically occupied, risking a second customer reserving a slot that's already full. Explicit invalidation on state change closes that window immediately instead of relying on staleness bounds alone."
/>

<QuizItem
  question="A hugely popular metro's L3 snapshot expires right as a Prime-Day spike hits it. What's the risk, and how does the design mitigate it?"
  answer="Every request that misses the cache in that instant could hit L4 Postgres simultaneously — a cache stampede. The mitigation is a single-flight recompute: only one request per region rebuilds the L3 snapshot on a miss, while the rest keep serving the slightly-stale snapshot until the rebuild finishes."
/>

### Open design challenge

Extend the network-scale design to support **dynamic, demand-aware
locker placement**: given historical utilization and dwell-time data per
station, design a subsystem that recommends where to add new locker
capacity (or reallocate compartment-size mix) in a city. What data would
you pipe from the Package and Reservation services into this subsystem,
how would you avoid it becoming a bottleneck on the hot reservation
path, and how does this connect to dwell-time prediction?

<details>
<summary>Reference answer</summary>

Add a **Placement Recommendation Service** that never sits on the
reservation hot path — it consumes an asynchronous event stream (a
Kafka-style topic) populated by state-change events the Package and
Reservation services already emit: `reservation-created`,
`deposit-confirmed`, `pickup-confirmed`, and `expired`/`returned`. Each
event carries station ID, compartment size, timestamps for reservation
and pickup, and outcome. The reservation hot path publishes to the
stream fire-and-forget; it never blocks on, or calls into, the
Placement Recommendation Service.

An hourly or nightly batch job aggregates that stream (or a warehouse
table fed by change-data-capture from Postgres, not the primary
transactional database itself) into two things per station and
compartment size: **utilization** (fraction of compartments of that size
occupied, sampled hourly) and **dwell-time percentiles** (p50/p90 time
from deposit to pickup). A separate dwell-time prediction model —
a regression over package size, day of week, and the station's
historical dwell distribution — feeds forward-looking estimates into the
same aggregation, so the recommendation isn't just reacting to what
already happened but anticipating demand for the size mix a station will
need next month.

The scoring step compares observed-plus-predicted demand against
installed capacity per size class and produces concrete, station-level
output — e.g. "Station #4471: reallocate 10 small compartments to
medium; trailing-30-day small-slot utilization is 22% vs. medium-slot
utilization 96%" or "add ~40 compartments within a 1-mile radius of
(lat, lng), based on a sustained rejected-reservation rate in that
area." The isolation from the hot path comes from two things: the
subsystem only ever reads from the async stream or a downstream
warehouse copy, never the primary geo-sharded transactional database,
and its batch jobs run on their own schedule rather than in response to
individual reservation events.

</details>

<Rubric
  items={[
    "Named a specific new component (e.g. a Placement Recommendation Service), not just \"a subsystem\"",
    "Specified that the component consumes an async event stream rather than making synchronous calls into the reservation hot path",
    "Named at least two concrete signals fed into it (e.g. reservation-created, deposit-confirmed, pickup-confirmed/dwell-time events)",
    "Explained why reading from a replica or warehouse copy, not the primary transactional database, avoids becoming a bottleneck",
    "Connected the design to dwell-time prediction: what it predicts from, and what decision it feeds",
    "Gave a concrete example recommendation (a specific station or area and a specific size-mix or capacity change), not a vague \"recommends changes\""
  ]}
/>
